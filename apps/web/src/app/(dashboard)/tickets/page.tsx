import StubPage from '../../../components/stub-page'

export default function TicketsPage() {
  return (
    <StubPage
      icon="▤"
      title="Tickets"
      description="Track execution tickets through the pipeline — backlog, queued, in progress, review, done."
    />
  )
}
