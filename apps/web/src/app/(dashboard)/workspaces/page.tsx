import StubPage from '../../../components/stub-page'

export default function WorkspacesPage() {
  return (
    <StubPage
      icon="▦"
      title="Workspaces"
      description="Multi-tenant workspace isolation with resource scoping and access control boundaries."
    />
  )
}
