import StubPage from '../../../components/stub-page'

export default function MemoryPage() {
  return (
    <StubPage
      icon="◇"
      title="Memory Graph"
      description="Explore the brain's memory tiers — core, recall, and archival — with vector search."
    />
  )
}
