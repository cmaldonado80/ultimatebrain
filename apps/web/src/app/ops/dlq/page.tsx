import StubPage from '../../../components/stub-page'

export default function DLQPage() {
  return (
    <StubPage
      icon="⚠"
      title="Dead Letter Queue"
      description="Inspect and retry failed jobs — ticket executions, cron runs, and webhook deliveries."
    />
  )
}
