import StubPage from '../../../components/stub-page'

export default function CheckpointsPage() {
  return (
    <StubPage
      icon="⟲"
      title="Checkpoints"
      description="State snapshots for agent execution — restore, compare, and debug from any checkpoint."
    />
  )
}
