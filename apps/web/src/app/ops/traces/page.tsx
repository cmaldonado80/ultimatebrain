import StubPage from '../../../components/stub-page'

export default function TracesPage() {
  return (
    <StubPage
      icon="⋯"
      title="Traces"
      description="Distributed tracing for agent executions — spans, latency, and dependency graphs."
    />
  )
}
