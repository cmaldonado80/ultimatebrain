import StubPage from '../../../components/stub-page'

export default function GatewayPage() {
  return (
    <StubPage
      icon="⇄"
      title="Gateway"
      description="LLM Gateway metrics — request volume, latency, cost tracking, and cache hit rates."
    />
  )
}
