{
  "name": "@solarc/hotel-ops-mini-brain",
  "version": "0.1.0",
  "private": true,
  "description": "Hospitality Mini Brain for the Solarc Brain platform — PMS, Revenue, Guest Profile, and domain agents",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "typecheck": "tsc --noEmit",
    "dev": "tsx watch src/server/brain-bridge.ts"
  },
  "dependencies": {
    "@solarc/brain-sdk": "workspace:*",
    "drizzle-orm": "^0.36.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.6.0"
  }
}
