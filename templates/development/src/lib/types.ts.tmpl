/**
 * {{DOMAIN_TITLE}} — shared types
 *
 * Define your domain-specific request/response types here.
 * These mirror the contract from the {{DOMAIN_TITLE}} Mini Brain.
 */

export interface DomainInput {
  input: string
  // TODO: Replace with real domain fields
}

export interface DomainResponse {
  domain: string
  result: unknown
  computedAt: string
  // TODO: Replace with real domain response shape
}
